services:
  - type: web
    name: llm-quiz-solver-gautham
    env: python
    region: oregon
    buildCommand: "pip install -r requirements.txt && playwright install chromium && playwright install-deps"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: STUDENT_EMAIL
        value: 24f1002265@ds.study.iitm.ac.in
      - key: STUDENT_SECRET
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: GOOGLE_API_KEY
        sync: false
    healthCheckPath: /health